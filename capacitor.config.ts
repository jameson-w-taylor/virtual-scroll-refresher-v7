import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'virtual-scroll-refresher-v7',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
